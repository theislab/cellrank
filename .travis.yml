language: python
python:
    - "3.6"
    - "3.7"
    - "3.8"
env:
    - MPLBACKEND=Agg
install:
    - pip install vendorize future_fstrings
    - python-vendorize  # clones msmtools
    - pip install -e .[test]
script:
    - pytest
